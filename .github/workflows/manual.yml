name: Push Event
on:
  push:
    branches:
    - master
env: 
  BOO: 'ahhh'
  UNICORN: 'real'
jobs:
  test:
    if: "${{ env.BOO == 'ahhh' }}" 
    name: Run tests
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@master
    - run: echo "hello world"

