name: Test

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - if: ${{ success() }}
      uses: ouzi-dev/commit-status-updater@v1.1.0
      with:
        name: "what is it"
        status: "error"  
