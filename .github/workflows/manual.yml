name: Push Event
on:
  push:
    branches:
    - master
jobs:
  test_2:
    services:
      neo4j:
        image: neo4j
    
    runs-on: ubuntu-latest
    env: 
      foo: "bar"
    steps:
    - name: checkout
      uses: actions/checkout@v2
