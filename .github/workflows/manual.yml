name: xcode
on:
  push:
jobs:
  build:
    runs-on: ubuntu-latest
    services:
      rabbitmq:
        image: rabbitmq
        options: |-
          --health-cmd 'redis-cli ping'
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    steps:
    - name: checkout
      uses: actions/checkout@v2
